#include <cstdio>
#include <iostream>
#include <cstring>

using namespace std;

int main(){
    char a[1005], b[1005];
    int i;
    while(~scanf("%s",a)){
        if(strcmp(a, "#") == 0) return 0;
        memset(b,'0',1005*sizeof(char));
        scanf("%s",b);
        int m = strlen(b);
        for(i = 0; i < strlen(a); i++){

        }
    }
return 0;
}
